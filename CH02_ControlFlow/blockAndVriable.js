// {## Variables and Block Scope}

// Scope >>- Area where variable is defined or accessible

let score = 90; //global

console.log(score);
if (true) {
  score = 50;
  console.log(score);
}
